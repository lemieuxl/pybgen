language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
install:
  - pip install -U pip
  - pip install -U setuptools
  - pip install -U six
  - pip install -U numpy
  - pip install -U coveralls
script:
  - coverage run setup.py test
after_success:
  - coveralls
